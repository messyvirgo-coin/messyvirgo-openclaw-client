services:
  openclaw-gateway:
    # Bridge port (expose only on localhost; gateway port is in docker-compose.yml)
    ports:
      - "127.0.0.1:${OPENCLAW_BRIDGE_PORT:-18790}:${OPENCLAW_BRIDGE_PORT:-18790}"
