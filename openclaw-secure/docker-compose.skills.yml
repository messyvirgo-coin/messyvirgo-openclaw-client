# skills/ lives at repo root; compose files are in openclaw-secure/
services:
  openclaw-gateway:
    volumes:
      - ../skills:/home/node/custom-skills:ro
  openclaw-cli:
    volumes:
      - ../skills:/home/node/custom-skills:ro
